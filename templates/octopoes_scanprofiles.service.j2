[Unit]
Description=OpenKat Octopoes-scanprofiles in Docker-compose ({{ openkat_service_root }}/octopoesscanprofiles)
Requires=docker.service
After=docker.service
Requires=traefik.service
Requires=docker-compose-networks.service
Requires=openkat_rabbitmq.service
Requires=openkat_crux.service

[Service]
Restart=always
WorkingDirectory={{ openkat_service_root }}/octopoesscanprofiles
TimeoutStartSec=2min
ExecStartPre={{ docker_binary }} compose  -f {{ openkat_service_root }}/octopoesscanprofiles/docker-compose.yml pull
ExecStart={{ docker_binary }} compose -f {{ openkat_service_root }}/octopoesscanprofiles/docker-compose.yml up
ExecStop={{ docker_binary }} compose -f {{ openkat_service_root }}/octopoesscanprofiles/docker-compose.yml stop

[Install]
WantedBy=multi-user.target
