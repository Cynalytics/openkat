---
- name: Create srv directory
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: '0755'
  with_items:
    - "{{ openkat_service_root }}/"
    - "{{ openkat_service_root }}/octopoesapi/"

- name: Create octopoes compose file
  ansible.builtin.template:
    src: "octopoes_api_docker-compose.yml.j2"
    dest: "{{ openkat_service_root }}/octopoesapi/docker-compose.yml"
    owner: root
    group: root
    mode: '0640'
  notify: Restart openkat_octopoesapi service

- name: Create systemd Unit file for octopoes service
  ansible.builtin.template:
    src: "octopoes_api.service.j2"
    dest: /etc/systemd/system/openkat_octopoesapi.service
    mode: '0644'
  notify:
    - Reload systemd
    - Restart openkat_octopoesapi service

- name: Enable openkat_octopoesapi service
  ansible.builtin.systemd:
    name: openkat_octopoesapi.service
    enabled: true
    daemon_reload: true # In case the service file was changed
    state: started
    no_block: true

- name: Wait until octopoesapi service is active (might take a few tries as it needs to pull the docker image on first run)
  ansible.builtin.systemd:
    name: openkat_octopoesapi.service
  register: svc_state
  retries: 25
  delay: 10
  until: svc_state.status.ActiveState == 'active'
  changed_when: false
  when: not ansible_check_mode
